<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- Other manifest declarations -->
    <!-- Add permissions here -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Todamoon_v1"
        tools:targetApi="31">
        <activity
            android:name=".Driver.QueueingActivity"
            android:exported="false" />
        <activity
            android:name=".Passenger.RegisterPassenger"
            android:exported="false" />
        <activity
            android:name=".Passenger.LoginPassenger"
            android:exported="false" />
        <activity
            android:name=".Passenger.PassengerMainUI"
            android:exported="false" />
        <activity
            android:name=".Driver.DriverMainUI"
            android:exported="false" />
        <activity
            android:name=".Driver.LoginDriver"
            android:exported="false" />
        <activity
            android:name=".Driver.RegisterDriver"
            android:exported="false" />
        <activity
            android:name=".FrontPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>